Project works only for MP4 file formats.

**Installation**

1. Clone the repo
   > git clone https://github.com/multeders/simple_video_editor.git
2. Install requirements txt
   >  pip install -r requirements.txt
3. Run program
   > python main.py
